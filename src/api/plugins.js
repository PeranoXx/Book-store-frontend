// import { AxiosRequestConfig, AxiosResponse } from "axios";

// export const addToken = (req: AxiosRequestConfig) => {
//   if (store.getters.token) {
//     req.headers = {Authorization : `Bearer ${store.getters.token}`};
//   }

//   return req;
// };
